# Enforce LF line endings in repo and ensure consistent behavior across platforms
* text=auto eol=lf

# Explicitly enforce LF for key files
README.md text eol=lf
bucket/*.json text eol=lf

# Do not modify binary files
*.exe -text
*.zip -text
*.tar.gz -text
*.7z -text
